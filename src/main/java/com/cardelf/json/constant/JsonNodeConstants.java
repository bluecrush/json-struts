package com.cardelf.json.constant;

/**
 * The type Json schema constants.
 *
 * @author bluecrush
 */
public class JsonNodeConstants {

    /**
     * jsonSchema中节点id拼接符
     */
    public static final String NODE_ID_SPLICE = "/";

    /**
     * jsonSchema中节点id拼接符(数组根节点)
     */
    public static final String NODE_ARRAY_ITEMS = "items";


    /**
     * jsonSchema中节点jsonpath前缀符号
     */
    public static final String NODE_JSONPATH_PREFIX = "$";

    /**
     * jsonSchema中节点jsonpath拼接符
     */
    public static final String NODE_JSONPATH_SPLICE = ".";

    /**
     * jsonSchema中节点jsonpath拼接符正则
     */
    public static final String NODE_JSONPATH_SPLICE_RG = "\\.";


    /**
     * jsonSchema中节点json path拼接符
     */
    public static final String NODE_JSONPATH_ARRAY = "[*]";


    /**
     * jsonSchema中节点json path拼接符
     */
    public static final String NODE_PATTEN_STRING = "^.*$";

    
}
